# tour-sandbox

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tour-sandbox` to build the library.

## Running unit tests

Run `nx test tour-sandbox` to execute the unit tests via [Vitest](https://vitest.dev/).
