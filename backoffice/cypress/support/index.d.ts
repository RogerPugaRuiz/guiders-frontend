/// <reference types="cypress" />

declare namespace Cypress {
  interface Chainable {
    // Puedes agregar comandos personalizados aquí si los necesitas
    // login(email: string, password: string): Chainable<Element>;
  }
}
